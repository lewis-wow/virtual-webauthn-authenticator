# Docker BuildKit Configuration
# Enable BuildKit for faster builds and advanced features
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# BuildKit progress output (auto, plain, tty)
BUILDKIT_PROGRESS=auto
