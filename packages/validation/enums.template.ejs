/**
 * @file Automatically generated by a custom script.
 */

import { z } from 'zod';
import { <%= enumNames.join(', ') %> } from '<%= importAlias %>';

<%- enumNames.map(name => `export const ${name}Schema = z.enum(${name});`).join('\n\n') %>
